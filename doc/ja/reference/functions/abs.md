# abs

`(abs number)`

`number` の絶対値を返します。数値型は保持され、整数は整数を、浮動小数点数は浮動小数点数を、比は比を返します。複素数の引数は浮動小数点数の modulus を返します。完全な部分に対しても実数です。

```lisp
(abs -5) ; => 5
```

```lisp
(abs -3.14) ; => 3.14
```

```lisp
(abs #c(3 4)) ; => 5.0
```
