# phase

`(phase number)`

`number` の複素平面上の偏角を浮動小数点数で返す。複素数なら虚部と実部の `atan2`、非負の実数なら `0.0`、負の実数なら円周率である。4バックエンドすべてで動作する（WASM バックエンドは超越関数をソフトウェアで計算するため、最下位ビットが異なる場合がある）。

```lisp
(phase 5) ; => 0.0
```

```lisp
(phase -5) ; => 3.141592653589793
```
